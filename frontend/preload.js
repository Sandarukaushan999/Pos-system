// Preload script for Electron (secure, contextIsolation enabled)
// No Node.js APIs exposed to renderer
// Ready for future IPC if needed
 
window.addEventListener('DOMContentLoaded', () => {
  // You can expose safe APIs here if needed in the future
}); 